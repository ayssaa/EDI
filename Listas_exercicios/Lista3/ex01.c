// Porque elas são basicamente listas simplesmente encadeadas, porém com regras durante a inserção, remoção e busca. 
// Nas listas encadeadas, poderíamos inserir no final ou no início, podemos remover o valor que quisermos. Porém não 
// podemos fazer isso quando implementamos pilhas ou filas, pois isso fere o encapsulamento do TAD, ou seja, são basicamente 
// listas com restrições!